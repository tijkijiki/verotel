services:
  verotel-db:
    image: postgres:16
    environment:
      POSTGRES_DB: verotel
      POSTGRES_USER: verotel
      POSTGRES_PASSWORD: verotel
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U verotel"]
      interval: 1s
      retries: 30
