datasources {
  default {
    driverClassName = "org.testcontainers.jdbc.ContainerDatabaseDriver"
    url = "jdbc:tc:postgresql:16:///verotel-test"
    username = "verotel-test"
    password = "verotel-test"
    dialect = POSTGRES
  }
}
liquibase {
  datasources {
    default {
      change-log: "classpath:db/liquibase-changelog.xml"
    }
  }
}
micronaut {
  server {
    port: -1
  }
}
